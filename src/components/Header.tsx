import React from "react";

const Header = () => {
  return (
    <header className="p-3.5 bg-transparent backdrop-blur-md w-full sticky top-0 shadow-sm">
      Header
    </header>
  );
};

export default Header;
